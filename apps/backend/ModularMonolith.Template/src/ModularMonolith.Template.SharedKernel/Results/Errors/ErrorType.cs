namespace ModularMonolith.Template.SharedKernel.Results.Errors
{
    public enum ErrorType
    {
        Validation,
        Problem,
        NotFound,
        Conflict,
        None,
        NullValue
    }
}
